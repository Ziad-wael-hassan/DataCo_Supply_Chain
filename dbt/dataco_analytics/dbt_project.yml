name: 'dataco_analytics'
version: '1.0.0'
config-version: 2

profile: 'dataco'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dataco_analytics:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: warehouse
      +materialized: table
